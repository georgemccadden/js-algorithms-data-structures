function binarySearch(target, nums) {
    // First you want to see if the target appears in nums collection

    // 'floorIndex' and 'ceilingIndex' are the parameters of the collection of nums
    // -1 means the parameter before the first number in the collection and nums.length is the parameter after the last number in the collection
}